<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{desc}}">
    <title>{{title}}</title>
</head>
<body>

    <div id="alertBoxLogin" class="alertBox text-white bg-teal-500 p-4 rounded-lg transition-all duration-300 z-40"></div>

<div class="flex w-96 mx-auto items-center justify-center h-screen ">

    <form id="loginForm" action="/sign-in" method="POST" class="login-form flex flex-col p-10 rounded-3xl w-11/12 lg:w-full box-border bg-slate-700 shadow-lg border-e-4 border-l-4 border-teal-400 text-white poppins">
    <img src="/assets/images/xiao-main.png" width="250" class="mx-auto" alt="">
        <label for="email" class="font-regular text-sm mt-3">Email</label>
        <input type="email" name="email" id="email" class="p-2 border-t-2 border-b-2 placeholder:opacity-50 bg-slate-700 border-teal-700 rounded-md mt-1 mb-3 outline-none focus:border-teal-300" placeholder="dawnwinery@gmail.com" required>
        <label for="password" class="font-regular text-sm mt-3">Password</label>
        <input type="password" name="password" id="password" class="p-2 border-t-2 placeholder:opacity-50 border-b-2 bg-slate-700 border-teal-700 rounded-md mt-1 mb-3 outline-none focus:border-teal-300" placeholder="jean@123" required>
        <button type="submit" id="login" class="btn py-3 border-4 border-teal-400 rounded-2xl hover:bg-teal-400 mt-3 font-regular transition-colors ease-linear font-semibold">Log In</button>
    </form>
</div>

{{#if messages.success}}
  <script>
    window.addEventListener('load', function () {
      showAlertBoxLogin('{{messages.success}}')
    })
  </script>
  {{/if}}

<script defer src="/js/login.js"></script>
</body>
</html>
